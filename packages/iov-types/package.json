{
  "name": "@iov/types",
  "version": "0.0.0",
  "description": "General types for IOV projects",
  "author": "will-iov <will-iov@users.noreply.github.com>",
  "license": "AGPL-3.0",
  "main": "index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/iov-one/web4/tree/master/packages/iov-types"
  },
  "scripts": {
    "run-dev": "ts-node",
    "format": "prettier --write --loglevel warn './types/**/*.ts'",
    "lint": "tslint -t verbose --project .",
    "lint-fix": "yarn lint --fix",
    "test": "echo 'Info: no test specified'",
    "prebuild": "yarn format && yarn lint",
    "build": "echo 'Info: nothing to be built'",
    "build-or-skip": "[ -n \"$SKIP_BUILD\" ] || yarn build"
  },
  "dependencies": {
    "@types/long": "^4.0.0",
    "@types/node": "^9.6.6",
    "long": "^4.0.0",
    "xstream": "^11.4.0"
  }
}
