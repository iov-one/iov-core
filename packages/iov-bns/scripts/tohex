#!/bin/bash
set -o errexit -o nounset -o pipefail
command -v shellcheck > /dev/null && shellcheck "$0"

# take input from a file if given, otherwise stdin
IN=${1:--}

cat $IN | od -x | cut -c9- | tr -d ' \n'
echo
