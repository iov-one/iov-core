os:
  - linux
  - osx

language: node_js
node_js:
  - "8"

script:
- |
  if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then
    # Test browser
    cd packages/testable-demo && yarn test-safari
  else
    yarn build && yarn test
  fi

notifications:
  email: false
